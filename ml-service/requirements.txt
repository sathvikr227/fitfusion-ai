# CPU-only torch keeps the Render image small; the default index would pull CUDA.
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Web layer ────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2

# ── Models 1 & 3: XGBoost + SHAP ─────────────────────────────────────────────
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.0
xgboost==3.4.1
shap==0.52.0
joblib==1.6.0

# ── Model 4: Transformer ─────────────────────────────────────────────────────
torch==2.14.0+cpu

# ── Model 5: PPO agent ───────────────────────────────────────────────────────
stable-baselines3==2.9.0
gymnasium==1.3.0

# ── Dev / training only ──────────────────────────────────────────────────────
pytest==8.3.3
matplotlib==3.11.1
